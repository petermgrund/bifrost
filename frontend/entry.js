// Bundle entry for the vendored @material/web components.
//
// `all.js` registers the full Material 3 component set. That keeps the spike
// simple; to shrink the bundle later, replace this with individual imports,
// e.g. `import '@material/web/button/filled-button.js';`.
import '@material/web/all.js';
